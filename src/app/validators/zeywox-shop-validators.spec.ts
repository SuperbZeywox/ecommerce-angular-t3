import { ZeywoxShopValidators } from './zeywox-shop-validators';

describe('ZeywoxShopValidators', () => {
  it('should create an instance', () => {
    expect(new ZeywoxShopValidators()).toBeTruthy();
  });
});
